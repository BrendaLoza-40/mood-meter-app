
  # Kiosk - Mood Tracker App

  This is a code bundle for the Kiosk component of the Mood Tracker system.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  